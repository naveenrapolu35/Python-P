a = "AAACCCBAABB"
result = ""
count = 1
for i in range(len(a)):
    # Check next character safely
    if i < len(a) - 1 and a[i] == a[i + 1]:
        count += 1
    else:
        result += a[i] + str(count)
        count = 1  # reset
print(result)
